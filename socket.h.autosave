#ifndef SOCKET_H
#define SOCKET_H

#include <winsock2.h>
#include <windows.h>
#include <QLabel>

class socket
{
public:
    socket();
    bool start();
protected:
    DWORD WINAPI connectToServer(QLabel*);
private:
};

#endif // SOCKET_H
